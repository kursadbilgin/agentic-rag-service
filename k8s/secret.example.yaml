# Template only — never commit real keys. Create the real secret imperatively:
#   kubectl -n agentic-rag create secret generic agentic-rag-secrets \
#     --from-literal=ANTHROPIC_API_KEY=sk-... \
#     --from-literal=GOOGLE_API_KEY=
apiVersion: v1
kind: Secret
metadata:
  namespace: agentic-rag
  name: agentic-rag-secrets
type: Opaque
stringData:
  ANTHROPIC_API_KEY: ""
  GOOGLE_API_KEY: ""
